import { eflPublicSignupReadiness } from '../lib/efl-auth.js';
export default async function handler(req,res){res.setHeader('Cache-Control','no-store, max-age=0');res.setHeader('X-Content-Type-Options','nosniff');if(req.method!=='GET')return res.status(405).json({error:'Method not allowed'});try{const ready=await eflPublicSignupReadiness();return res.status(200).json({signInAvailable:ready.coreConfigured,passwordResetAvailable:ready.coreConfigured&&ready.emailConfigured&&ready.verified,publicSignupAvailable:ready.publicSignupEnabled});}catch{return res.status(503).json({signInAvailable:false,passwordResetAvailable:false,publicSignupAvailable:false});}}
